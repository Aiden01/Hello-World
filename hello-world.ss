.:
    This version is for Snow*
:.

§ Main function. Parameters unrequired.
int32 main(int32 argc, char** argv) {
    § We just need to print "hello world!"
    
    print("hello world!");
    
    § and we return from the function with an `int32`
    
    return 0;
}
